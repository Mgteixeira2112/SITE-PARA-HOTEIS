# GitHub Pages must be configured in Settings > Pages with Source = GitHub Actions.
# The transformation branch is intentionally published as the live homologation environment
# so visual/operational changes can be tested continuously without touching the NOVOHOTEL repo.
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - refactor/novohotel-saas-simplificado
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Build
        run: npm run build

      - name: Verify build output
        run: |
          test -f dist/index.html
          test -f dist/manifest.webmanifest
          grep -q '/SITE-PARA-HOTEIS/assets/' dist/index.html
          ! grep -q 'src="/src/main.tsx"' dist/index.html
          find dist -maxdepth 2 -type f | sort | head -100

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy Vite dist to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
